// Write a program that:
// Declares an integer variable x and assigns it a value.
// Prints the value of x.
// Uses comments to explain what each line is doing.

#include<stdio.h> //preprocessor directive - includes external librarires before the execution begins so that the programs run smoothly
int main(){
    int x=10; //assigning value 10 to x
    printf("Value of X = %d\n",x); //printing the value of x and adding next line for visual appearance
    return 0;
}