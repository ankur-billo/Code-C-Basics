// Write a program where you:
// Print a simple message.
// Change the return value of main() from 0 to another number (like 5).
// Observe what happens when you run the program.

#include<stdio.h>
int main(){
    printf("I'm experementing with return statement\n");
    return 5; //It means the program doesn't execute successfully, it returns the value doesn't show in the ouput but pass to the OS
}